# Python Code Review Prompt - Подробная документация

## 📖 Описание

Это профессиональный **адаптивный** XML-промпт для комплексного ревью Python кода, который анализирует код на соответствие:
- **Google Python Style Guide** (официальный стандарт Google)
- **The Hitchhiker's Guide to Python** (best practices сообщества)
- **Docker/Container Best Practices** (для контейнерных окружений)

Промпт предназначен для использования с AI-ассистентами (Claude, GPT-4, etc.) и обеспечивает глубокий анализ кода с практическими рекомендациями.

---

## 🎯 Что делает промпт

### 1. **Адаптивный анализ сложности кода**

Промпт автоматически определяет сложность проверяемого кода:

#### Для **простого кода** (без БД, API, concurrency):
Фокусируется на критически важных аспектах:
- ✓ Корректность логики и граничные случаи
- ✓ Обработка ошибок
- ✓ Валидация входных данных
- ✓ Управление ресурсами (файлы, память)
- ✓ Ясность и читаемость кода
- ✓ Базовая безопасность
- ✓ Тестируемость

#### Для **сложного кода** (с БД, API, высокой нагрузкой):
Добавляет расширенные проверки:
- Performance и O-notation
- Concurrency и race conditions
- Database оптимизация
- API design и версионирование

---

### 2. **Контекстный анализ (ОПЦИОНАЛЬНО)**

**Автоматически активируется** когда:
- Код неясен или архитектурные решения вызывают сомнения
- Сложная бизнес-логика требует понимания контекста
- Есть несколько вариантов реализации и нужен выбор
- Критичные решения по безопасности или производительности

**AI задает уточняющие вопросы:**

#### 🎯 **Purpose (Цель кода):**
- Обработка данных (ETL, парсинг, трансформация)
- API сервис (REST, GraphQL, gRPC)
- CLI инструмент (скрипт, утилита)
- Библиотека (для других разработчиков)
- Фоновая задача (worker, cron job)

#### 👥 **Users (Пользователи):**
- Developers (внутренние разработчики)
- End-users (конечные пользователи)
- Системы (другие сервисы, автоматизация)
- Администраторы

#### ⚡ **Criticality (Критичность):**
- Production critical (падение = инцидент)
- Business important (влияет на бизнес-процессы)
- Internal tool (для команды)
- Prototype/POC (исследование)
- Legacy maintenance (поддержка старого кода)

#### 📊 **Load (Ожидаемая нагрузка):**
- Единичные запросы (ручной запуск)
- Low (десятки запросов в минуту)
- Medium (сотни-тысячи RPS)
- High (десятки тысяч RPS)
- Batch processing (большие объемы данных)

#### 📋 **Requirements (Специфичные требования):**
- Compliance (GDPR, HIPAA, PCI-DSS)
- Security constraints (аутентификация, авторизация, шифрование)
- Performance SLA (latency, throughput)
- Data retention (как долго хранить)
- Integration requirements (внешние системы)

#### 🌍 **Environment (Окружение выполнения):**
- Local development
- CI/CD pipeline
- Docker/Kubernetes
- Serverless (Lambda, Cloud Functions)
- Bare metal servers

**Влияние контекста на ревью:**
```
[INFO] Контекстный анализ активирован

Purpose: API сервис для обработки платежей
Criticality: Production critical
Load: Medium (1000 RPS)
Requirements: PCI-DSS compliance

Адаптированные рекомендации:
  [CRITICAL] Добавить rate limiting (защита от DDoS)
  [CRITICAL] Логирование без sensitive данных (PCI-DSS)
  [IMPORTANT] Circuit breaker для внешних API
  [IMPORTANT] Distributed tracing для debugging
```

---

### 3. **Docker/Container Environment (АВТОМАТИЧЕСКАЯ ДЕТЕКЦИЯ)**

**Автоматически детектит признаки Docker по:**
- 📄 Файлам: Dockerfile, docker-compose.yml, .dockerignore
- 💻 Коду: os.getenv(), os.environ, ENV комментарии
- 📦 Импортам: logging, signal, sys
- 🔧 Паттернам: PORT, HOST, DATABASE_URL в переменных

**Когда обнаружено - активирует 9 специфичных проверок:**

#### 🔴 **[CRITICAL] Graceful Shutdown**
```python
# [FAIL] Нет обработки SIGTERM
def main():
    while True:
        process_tasks()  # Контейнер убьется принудительно

# [OK] Graceful shutdown
import signal
shutdown_requested = False

def signal_handler(sig, frame):
    global shutdown_requested
    logger.info('Shutting down gracefully...')
    shutdown_requested = True

signal.signal(signal.SIGTERM, signal_handler)
signal.signal(signal.SIGINT, signal_handler)

def main():
    while not shutdown_requested:
        process_tasks()
    cleanup_resources()
    sys.exit(0)
```

#### 🔴 **[CRITICAL] Secrets Management**
```python
# [FAIL] Пароли в ENV (видны в docker inspect)
DB_PASSWORD = os.getenv('DB_PASSWORD')

# [OK] Docker secrets (production)
def get_secret(secret_name):
    """Читает secret из Docker secrets."""
    try:
        with open(f'/run/secrets/{secret_name}') as f:
            return f.read().strip()
    except FileNotFoundError:
        return os.getenv(secret_name.upper())  # Fallback для dev

DB_PASSWORD = get_secret('db_password')
```

#### 🟡 **[IMPORTANT] Hardcoded Paths**
```python
# [FAIL] Абсолютные пути
LOG_FILE = '/app/logs/app.log'
DATA_DIR = '/var/data'

# [OK] Из ENV с defaults
LOG_FILE = os.getenv('LOG_FILE', '/dev/stdout')
DATA_DIR = Path(os.getenv('DATA_DIR', '/data'))
```

#### 🟡 **[IMPORTANT] Environment Variables**
```python
# [FAIL] Без default - упадет
DB_HOST = os.environ['DATABASE_HOST']
PORT = int(os.getenv('PORT'))  # None -> ValueError

# [OK] С default значениями
DB_HOST = os.getenv('DATABASE_HOST', 'localhost')
PORT = int(os.getenv('PORT', '8000'))
```

#### 🟡 **[IMPORTANT] Logging Strategy**
```python
# [FAIL] Логи в файл (теряются при рестарте)
logging.basicConfig(filename='/app/logs/app.log')

# [OK] В stdout для Docker
import sys
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s',
    stream=sys.stdout  # Контейнер захватит stdout
)
```

#### 🟡 **[IMPORTANT] Health Checks**
```python
# [OK] Health check для Kubernetes/Docker
@app.route('/health')
def health_check():
    """Health check endpoint для orchestrator."""
    try:
        db.execute('SELECT 1')
        redis.ping()
        return {'status': 'healthy'}, 200
    except Exception as e:
        return {'status': 'unhealthy', 'error': str(e)}, 503
```

#### 🟡 **[IMPORTANT] Network Configuration**
```python
# [FAIL] localhost не работает в Docker network
DB_HOST = 'localhost'
REDIS_HOST = '127.0.0.1'

# [OK] Service names из docker-compose
DB_HOST = os.getenv('DB_HOST', 'postgres')
REDIS_HOST = os.getenv('REDIS_HOST', 'redis')

# [OK] Bind на 0.0.0.0 внутри контейнера
app.run(host='0.0.0.0', port=PORT)  # Не 127.0.0.1!
```

#### 🟡 **[IMPORTANT] File Permissions**
```python
# Dockerfile - [FAIL]
FROM python:3.11
COPY . /app
CMD ["python", "app.py"]  # Запуск от root!

# Dockerfile - [OK]
FROM python:3.11
RUN useradd -m -u 1000 appuser
WORKDIR /app
COPY --chown=appuser:appuser . .
USER appuser
CMD ["python", "app.py"]
```

#### 🟡 **[IMPORTANT] Resource Limits**
```python
# [FAIL] Загрузка всего в память
def process_large_file(filename):
    data = open(filename).read()  # Может быть гигабайты!
    return process(data)

# [OK] Streaming обработка
def process_large_file(filename):
    with open(filename) as f:
        for line in f:
            yield process(line)
```

**Bonus: Dockerfile Review** (если файл существует)
```
[INFO] Обнаружен Dockerfile - запущен дополнительный анализ

Проверки:
  [WARN] Не используется multi-stage build (размер образа 1.2GB)
  [FAIL] Отсутствует .dockerignore (копируются .git, __pycache__)
  [INFO] Base image: python:3.11 (можно python:3.11-slim -60% размера)
  [OK] Layer caching оптимизирован (COPY requirements.txt перед COPY .)
  [CRITICAL] Уязвимости в base image: CVE-2023-XXXX (critical)
```

---

### 4. **Архитектурный анализ**

#### **Структура репозитория:**
- Проверяет корректность организации файлов
- Находит повторяющиеся пути и излишнюю вложенность
- Рекомендует правильное размещение модулей, тестов, документации

#### **Структура кода:**
Выявляет антипаттерны:
- Циклические зависимости между модулями
- Spaghetti code (вложенные if/for)
- Ravioli code (сотни похожих классов)
- Скрытые связи и глобальное состояние

#### **Модули и импорты:**
- Проверяет правильность импортов
- Рекомендует использование namespace
- Контролирует структуру пакетов

---

### 5. **Документация кода**

#### **Автоматическое создание/улучшение docstrings:**
Для каждой функции создает docstring, понятный junior-разработчику:
```python
def setup_logger(rules, args):
    """
    Настраивает и возвращает логгер для валидатора.

    Функция создает логгер с настраиваемым уровнем и обработчиками.
    Если логгер уже настроен, возвращает существующий экземпляр.

    Args:
        rules (dict): Словарь конфигурации с разделом 'logging':
                      - 'level': уровень ('DEBUG', 'INFO', ...)
                      - 'file': bool, писать ли в файл
                      - 'log_file': путь к файлу логов
        args (argparse.Namespace): Аргументы CLI с флагами
                                   'debug' и 'verbose'.

    Returns:
        logging.Logger: Настроенный экземпляр логгера.

    Notes:
        - Уровень определяется: debug → verbose → config
        - Обработчики добавляются только при первом вызове
        - Директории создаются автоматически
    """
```

#### **Построчные комментарии:**
- Объясняет что делает каждая строка/блок
- Зачем это нужно
- Какие данные обрабатываются
- Какие side effects возникают
- Влияние на архитектуру проекта

#### **Анализ сложности:**
- Находит "намудренный" код
- Предлагает упрощение
- Показывает "гениальное" решение (простое, элегантное, pythonic)

---

### 6. **Стратегия тестирования**

#### **Правильные тесты:**
Генерирует примеры unittest для каждой функции:
```python
def test_setup_logger_with_debug_flag(self):
    """Проверяет установку DEBUG уровня при флаге debug."""
    args = argparse.Namespace(debug=True, verbose=False)
    logger = setup_logger(self.rules, args)
    self.assertEqual(logger.level, logging.DEBUG)

def test_setup_logger_with_empty_rules(self):
    """Проверяет обработку пустой конфигурации."""
    with self.assertRaises(KeyError):
        setup_logger({}, self.args)

def test_setup_logger_creates_log_file(self):
    """Проверяет создание файла логов."""
    with tempfile.TemporaryDirectory() as tmpdir:
        rules = {'logging': {'file': True, 'log_file': f'{tmpdir}/test.log'}}
        setup_logger(rules, self.args)
        self.assertTrue(os.path.exists(f'{tmpdir}/test.log'))
```

#### **Неправильные тесты (антипаттерны):**
Показывает что НЕ надо делать:
```python
# [FAIL] Жестко заданный путь
def test_logger_bad(self):
    logger = setup_logger(rules, args)
    self.assertTrue(os.path.exists('/home/user/app.log'))  # Не работает на других машинах!

# [FAIL] Без изоляции
def test_logger_bad2(self):
    setup_logger(rules, args)  # Модифицирует глобальное состояние
    # Следующий тест может сломаться!
```

---

### 7. **Проверка по Google Style Guide**

#### **Правила языка:**
- Lint (pylint с Google pylintrc)
- Импорты (правильная группировка и порядок)
- Исключения (использование, документирование)
- Глобальное состояние (избегание mutable globals)
- Comprehensions (максимум 1 for + 1 if)
- Default arguments (не использовать mutable)
- Properties (только тривиальные)
- True/False проверки (идиоматичные)
- Type annotations (современный синтаксис)

#### **Стилевые правила:**
- Длина строки (80 символов)
- Отступы (4 пробела, никогда табы)
- Пробелы вокруг операторов
- Docstrings (обязательны для публичного API)
- Строки (f-strings, join вместо +=)
- Именование (module_name, ClassName, function_name)
- Комментарии (объясняют "почему")
- Длина функций (<40 строк предпочтительно)
- TODO комментарии (формат с ссылкой на issue)

---

### 8. **Best Practices из Hitchhiker's Guide**

#### **ООП vs Функциональное программирование:**
- Когда использовать классы (GUI, игры, состояние)
- Когда использовать функции (stateless, pure functions)
- Преимущества pure functions

#### **Продвинутые концепции:**
- Decorators (separation of concerns)
- Context Managers (классовый и функциональный подход)
- Dynamic typing (избегание переиспользования имен)
- Mutable vs Immutable types (эффективная конкатенация строк)

---

### 9. **Модуляризация больших файлов**

**Триггер:** Автоматически активируется если код > 500 строк (без docstrings/imports)

**Процесс:**
1. Подсчет реальных LOC (Lines of Code)
2. Идентификация логических групп функций/классов
3. Определение boundaries между модулями
4. Предложение структуры каталогов

**Пример вывода:**
```
[INFO] Обнаружено 850 LOC - рекомендуется модуляризация

Before:
  single_file.py (850 LOC)

After:
  project/
    __init__.py
    core.py (200 LOC) - основная бизнес-логика
    models.py (150 LOC) - классы данных
    utils.py (100 LOC) - вспомогательные функции
    validators.py (150 LOC) - валидация
    handlers.py (200 LOC) - обработчики
    constants.py (50 LOC) - константы

Rationale:
  - core.py: Изолирует бизнес-логику от helpers
  - models.py: Data classes для type safety
  - validators.py: Переиспользуемая валидация
  
Migration Plan:
  1. Создать constants.py (безопасно, нет зависимостей)
  2. Выделить validators.py (используется везде)
  3. Создать models.py
  4. Разделить core.py и handlers.py
  5. Обновить импорты
  6. Запустить тесты
```

---

### 10. **Немедленные исправления**

Приоритизированные рекомендации с временными оценками:

#### **[CRITICAL] - Исправить НЕМЕДЛЕННО:**
```
[CRITICAL] file.py:42
Issue: eval() с пользовательским вводом
Fix:
  # Было
  result = eval(user_input)
  
  # Должно быть
  import ast
  result = ast.literal_eval(user_input)  # Безопасно для literals
  
Impact: Критическая уязвимость - возможно выполнение произвольного кода
Time: 5 минут
```

#### **[IMPORTANT] - Исправить в ближайшее время:**
```
[IMPORTANT] file.py:156
Issue: Файл не закрывается при ошибке
Fix:
  # Было
  f = open('data.txt')
  data = f.read()
  f.close()
  
  # Должно быть
  with open('data.txt') as f:
      data = f.read()
      
Impact: Утечка file descriptors при частых ошибках
Time: 2 минуты
```

#### **[QUICK-WIN] - Быстрое улучшение:**
```
[QUICK-WIN] file.py:89
Issue: Магическое число без объяснения
Fix:
  # Было
  if age > 18:
  
  # Должно быть
  ADULT_AGE = 18
  if age > ADULT_AGE:
      
Impact: Улучшает читаемость и maintainability
Time: 1 минута
```

---

### 11. **Детальная проверка для простого кода**

Для кода без БД, API, высокой нагрузки промпт особенно внимателен к:

#### **Correctness (Корректность):**
```python
# Находит проблемы:
def divide(a, b):
    return a / b  # [FAIL] Что если b == 0?

def get_first(items):
    return items[0]  # [FAIL] Что если список пустой?

# Предлагает исправления:
def divide(a, b):
    if b == 0:
        raise ValueError("Cannot divide by zero")
    return a / b

def get_first(items, default=None):
    return items[0] if items else default
```

#### **Error Handling:**
```python
# [FAIL] Нет обработки
def read_config(path):
    return json.load(open(path))

# [OK] С обработкой
def read_config(path):
    try:
        with open(path) as f:
            return json.load(f)
    except FileNotFoundError:
        logger.error(f"Config not found: {path}")
        return {}
    except json.JSONDecodeError as e:
        logger.error(f"Invalid JSON: {e}")
        return {}
```

#### **Input Validation:**
```python
# [FAIL] Нет валидации
def calculate_discount(price, percent):
    return price * (percent / 100)

# [OK] С валидацией
def calculate_discount(price, percent):
    if not isinstance(price, (int, float)) or price < 0:
        raise ValueError(f"Invalid price: {price}")
    if not isinstance(percent, (int, float)) or not 0 <= percent <= 100:
        raise ValueError(f"Invalid percent: {percent}")
    return price * (percent / 100)
```

#### **Edge Cases чек-лист:**
- Пустые коллекции ([], {}, "")
- None значения
- Нулевые значения (0, 0.0)
- Отрицательные числа где ожидаются положительные
- Очень большие значения
- Специальные символы в строках

---

### 12. **Формат вывода**

Промпт генерирует структурированный отчет:

#### **0. Контекстный анализ (если активирован)**
```
[INFO] Контекстный анализ

Purpose: REST API для управления заказами
Users: End-users (мобильное приложение)
Criticality: Production critical
Load: Medium (500-1000 RPS)
Requirements: 
  - PCI-DSS compliance (обработка платежей)
  - Latency < 200ms (SLA)
  - Audit logging (все операции)

Адаптированные рекомендации:
  [CRITICAL] Sensitive data (карты) не логировать
  [CRITICAL] Rate limiting против DDoS
  [IMPORTANT] Distributed tracing для SLA мониторинга
  [IMPORTANT] Circuit breaker для payment gateway
```

#### **0.1. Docker анализ (если обнаружен)**
```
[INFO] Docker окружение обнаружено

Detected indicators:
  - docker-compose.yml присутствует
  - os.getenv() используется (12 мест)
  - Dockerfile найден

Docker-specific проблемы:
  [CRITICAL] app.py:67 - Нет обработки SIGTERM (30 мин)
  [CRITICAL] config.py:23 - Пароль в ENV (5 мин)
  [IMPORTANT] logger.py:45 - Логи в файл вместо stdout (2 мин)
  [IMPORTANT] main.py:12 - Hardcoded 'localhost' (1 мин)

Dockerfile review:
  [FAIL] Запуск от root
  [WARN] Base image размер 1.2GB (можно -60%)
  [OK] Layer caching оптимизирован
```

#### **1. Архитектура и структура**
```
[INFO] Архитектура и структура
Grade: Требует улучшений

Структура проекта:
  [OK] Модули в корректных местах
  [FAIL] Циклическая зависимость между auth.py и user.py
  [WARN] Тесты смешаны с основным кодом

Модульность:
  Coupling: Высокий (7/10) - много взаимозависимостей
  Cohesion: Средний (5/10) - функции смешивают разные concerns
```

#### **2. Документация**
```
[INFO] Обзор документации

Существующие docstrings: 3/10 функций (30%)

Улучшенные docstrings:
  [Функция setup_logger - см. выше полный пример]

Построчные комментарии для сложных функций:
  [Детальное объяснение каждой строки]

Анализ сложности:
  Функция process_data() - слишком сложная (CCN=15)
  Предложение по упрощению: [код]
```

#### **3. Стратегия тестирования**
```
[INFO] Стратегия тестирования

Правильные тесты:
  [2-3 примера с объяснениями]

Неправильные тесты (НЕ делать так):
  [1-2 антипаттерна с объяснением]

Пробелы в покрытии:
  - Нет тестов для error handling
  - Не покрыты edge cases (пустые списки)
```

#### **4. Что сделано хорошо**
```
[OK] Сильные стороны кода

- Использование context managers для файлов
- Понятные имена переменных
- Type annotations для публичного API
- Правильная структура импортов
```

#### **5. Критические проблемы**
```
[CRITICAL] Критические проблемы (приоритет: ВЫСОКИЙ)

1. [CRITICAL] auth.py:45 - SQL инъекция (1 час)
2. [CRITICAL] utils.py:123 - eval() с пользовательским вводом (5 мин)
3. [CRITICAL] app.py:67 - Нет SIGTERM handler для Docker (30 мин)
4. [IMPORTANT] main.py:89 - Утечка file descriptors (2 мин)
```

#### **6. Рекомендации по улучшению**
```
[INFO] Рекомендации (приоритет: СРЕДНИЙ)

1. Разбить process_data() на 3 функции (1 час)
2. Вынести константы из кода (30 мин)
3. Добавить валидацию входных данных (2 часа)
4. Streaming обработка для больших файлов (3 часа)
```

#### **7. Стилевые замечания**
```
[WARN] Стилевые замечания (приоритет: НИЗКИЙ)

1. Строка 156 превышает 80 символов
2. Отсутствует пробел после запятой (строка 234)
3. Inconsistent naming: getUserData vs get_user_info
```

#### **8. Немедленные действия**
```
[INFO] Немедленные действия

[CRITICAL] 3 исправления - немедленно (1.5 часа)
[IMPORTANT] 5 исправлений - сегодня/завтра (6 часов)
[QUICK-WIN] 8 улучшений - в течение недели (4 часа)

Общее время на критичные исправления: ~2 часа
```

#### **9. Предложения рефакторинга**
```
[INFO] Предложения рефакторинга

Before: [Сложный код]
After: [Гениальное простое решение]
Benefits:
  - Читаемость: +40%
  - Производительность: +15%
  - Тестируемость: +60%
Migration: [Безопасный план перехода]
```

#### **10. Общая оценка**
```
[INFO] Общая оценка кода

Scores:
  Архитектура: 6/10
  Читаемость: 7/10
  Maintainability: 5/10
  Стандарты: 8/10
  Тестируемость: 4/10
  Docker-готовность: 3/10 (если применимо)

Готовность к продакшену: [WARN] С исправлениями

Рекомендуемые следующие шаги:
  1. Исправить 3 критичные уязвимости (2 часа)
  2. Добавить Docker graceful shutdown (30 мин)
  3. Добавить обработку ошибок (4 часа)
  4. Написать тесты для core функций (1 день)
  5. Разбить большие функции (3 часа)
```

---

## 🚀 Как использовать

### 1. **Подготовка:**
```xml
<!-- Скопируй весь XML промпт -->
<prompt>
  ...
</prompt>
```

### 2. **Отправь AI-ассистенту:**
```
[Вставь XML промпт]

Теперь проверь этот код:

[Вставь свой Python код]

[Опционально: если есть Dockerfile или docker-compose.yml - тоже вставь]
```

### 3. **Получи детальный анализ:**
AI проанализирует код и выдаст структурированный отчет со всеми секциями.

---

## 🎬 Примеры сценариев работы

### **Сценарий 1: Простой скрипт без Docker**
```python
# simple_calc.py
def calculate(x, y):
    return x + y

result = calculate(5, 3)
print(result)
```

**Результат:**
- ✅ Context questions НЕ задаются (код простой и понятный)
- ✅ Docker checks НЕ активируются (нет признаков)
- ✅ Обычное ревью по Google Style Guide
